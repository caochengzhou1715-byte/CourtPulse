# CourtPulse

Unity CourtPulse project archive is published in this repository's Releases.

Download both release parts, concatenate them in alphabetical order into `CourtPulse-project.tar.gz`, then extract the archive. Unity regenerates `Library`, `Logs`, `Temp`, and `UserSettings` when the project is opened.
